h4#thumbs-parameters Thumbs Parameters
table.params-table
  thead
    tr
      th Parameter
      th Type
      th Default
      th Description
  tbody
    tr
      td thumbs
      td object
      td
      td
        p Object with thumbs component parameters. For example:
          pre
            code
              :code(lang="js")
                var mySwiper = new Swiper('.swiper-container', {
                  ...
                  thumbs: {
                    swiper: thumbsSwiper
                  }
                });
    tr.subparam-open-row
      td(colspan="4") {
    tr.subparam-row
      td swiper
      td object<br>Swiper
      td
      td Swiper instance of swiper used as thumbs or object with Swiper parameters to initialize thumbs swiper. For example:
        pre
          code
            :code(lang="js")
              var thumbsSwiper = new Swiper('.swiper-container-thumbs', {
                slidesPerView: 5,
              });

              var mySwiper = new Swiper('.swiper-container', {
                ...
                thumbs: {
                  swiper: thumbsSwiper
                }
              });
        p or
        pre
          code
            :code(lang="js")
              var mySwiper = new Swiper('.swiper-container', {
                ...
                thumbs: {
                  swiper: {
                    el: '.swiper-container-thumbs',
                    slidesPerView: 5,
                    ...
                  }
                }
              });
    tr.subparam-row
      td slideThumbActiveClass
      td string
      td 'swiper-slide-thumb-active'
      td Additional class that will be added to activated thumbs swiper slide
    tr.subparam-row
      td thumbsContainerClass
      td string
      td 'swiper-container-thumbs'
      td Additional class that will be added to thumbs swiper-container
    tr.subparam-row
      td multipleActiveThumbs
      td boolean
      td true
      td When enabled multiple thumbnail slides may get activated
    tr.subparam-row
      td autoScrollOffset
      td number
      td 0
      td Allows to set on which thumbs active slide from edge it should automaticall move scroll thumbs. For example, if set to <code>1</code> and last visible thumb will be activated (1 from edge) it will auto scroll thumbs

    tr.subparam-close-row
      td(colspan="4") }
