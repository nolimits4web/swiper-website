h4#lazy-parameters Lazy Loading Parameters
table.params-table
  thead
    tr
      th Parameter
      th Type
      th Default
      th Description
  tbody
    tr
      td lazy
      td object/boolean
      td
      td
        p Enables images lazy loading. Object with lazy loading parameters or boolean <b>true</b> to enable with default settings. For example:
          pre
            code
              :code(lang="js")
                var mySwiper = new Swiper('.swiper-container', {
                  lazy: {
                    loadPrevNext: true,
                  },
                });
    tr.subparam-open-row
      td(colspan="4") {
    tr.subparam-row
      td loadPrevNext
      td boolean
      td false
      td Set to "true" to enable lazy loading for the closest slides images (for previous and next slide images)
    tr.subparam-row
      td loadPrevNextAmount
      td number
      td 1
      td Amount of next/prev slides to preload lazy images in. Can't be less than <code>slidesPerView</code>
    tr.subparam-row
      td loadOnTransitionStart
      td boolean
      td false
      td By default, Swiper will load lazy images after transition to this slide, so you may enable this parameter if you need it to start loading of new image in the beginning of transition

    tr.subparam-row
      td elementClass
      td string
      td 'swiper-lazy'
      td CSS class name of lazy element
    tr.subparam-row
      td loadingClass
      td string
      td 'swiper-lazy-loading'
      td CSS class name of lazy loading element
    tr.subparam-row
      td loadedClass
      td string
      td 'swiper-lazy-loaded'
      td CSS class name of lazy loaded element
    tr.subparam-row
      td preloaderClass
      td string
      td 'swiper-lazy-preloader'
      td CSS class name of lazy preloader
    tr.subparam-close-row
      td(colspan="4") }
